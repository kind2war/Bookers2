<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

<!--ここからBootsstrap-->
  <body class="d-flex flex-column vh-100">
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand" href="/"><span>Bookers</span></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse"
                        data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <% if user_signed_in? %>

                <%= button_to user_path(current_user.id) , {method: :get, class: "btn nav-link"} do %>
                <i class="fas fa-home" style="color: #ffffff;"></i> Home
                <% end %>

                <%= button_to users_path, {method: :get, class: "btn nav-link"} do %>
                <i class="fas fa-users" style="color: #ffffff;"></i> Users
                <% end %>

                <%= button_to books_path, {method: :get, class: "btn nav-link"} do %>
                <i class="fas fa-book" style="color: #ffffff;"></i> Books
                <% end %>

                <%= button_to destroy_user_session_path, {method: :delete, class: "btn nav-link"} do %>
                <i class="fas fa-sign-out-alt" style="color: #ffffff;"></i> Log out
                <% end %>

              <% else %>

                <%= button_to '/' , {method: :get, class: "btn nav-link"} do %>
                <i class="fas fa-home" style="color: #ffffff;"></i> Home
                <% end %>

                <%= button_to '/home/about', {method: :get, class: "btn nav-link"} do %>
                <i class="fas fa-link" style="color: #ffffff;"></i> About
                <% end %>

                <%= button_to new_user_registration_path, {method: :get, class: "btn nav-link"} do %>
                <i class="fas fa-user-plus" style="color: #ffffff;"></i> Sign up
                <% end %>

                <%= button_to new_user_session_path, {method: :get, class: "btn nav-link"} do %>
                <i class="fas fa-sign-in-alt" style="color: #ffffff;"></i> Log in
                <% end %>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <% if flash[:notice] %>
     <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <main class="mb-auto">
    <%= yield %>
    </main>

    <footer class="pt-5 pb-4">
      <div class="container">
        <div class="row no-gutters">
          <div class="mx-auto">
            <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
