<!--
<%#= form_with model: book, url:, html: { method: :post }, local: true do |f| %>
  <%# if book.errors.any? %>
    <%#= book.errors.count %> errors prohibited this book from being saved:
      <ul>
        <%# book.errors.full_messages.each do |message| %>
        <li><%#= message %></li>
        <%# end %>
      </ul>
  <%# end %>
-->
<div>
<h2>New Book</h2>
  <div class="create">
    <%= form_with model: book do |f| %>　<!--部分テンプレ化する上で、User#showで表示させるには一体どうしたら・・・-->
    <div>
      <%= f.label :Title %>
      <%= f.text_field :title  %>
    </div>
    <div>
      <%= f.label :Opinion %>
      <%= f.text_area :opinion %></br>
    </div>
      <%= f.submit 'Create Books'%>
  </div>, placeholder:"感想など"
    <% end %>
<!--
<#% end %>
-->